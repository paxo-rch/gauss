#ifndef WIDGET_BUTTON_HPP
#define WIDGET_BUTTON_HPP

#define POINTER_DATA 1
#define INT_DATA 2

#define Button Label // while i have a probleme with the constructor

/*#include "label.hpp"

class Button : public Label
{
    public:
    Button(int16_t x, int16_t y, int16_t width, int16_t height)
    {
        init(x, y, width, height);
        setText("");
        setMarginX(0);
        setMarginY(0);
        setPaddingX(0);
        setPaddingY(0);
        setVerticalAlignment(CENTER_ALIGNMENT);
        setHorizontalAlignment(CENTER_ALIGNMENT);
    }

    void setEvent(void (*event)(void*), void* data);
    void setEvent(void (*event)(int), int dataNb);
};*/

#endif